import { GraphEditor } from "./graph-editor-api";

/**
 * @param {GraphEditor} graph 
 * @param {HTMLDivElement} container 
 */
export function registerControlPanel(graph, container) {
  // Create buttons and stuff
}
